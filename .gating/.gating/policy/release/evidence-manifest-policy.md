# Release evidence manifest policy

Release evidence checks are profile-enabled. Components can require files such as `MANIFEST.json`, `composer.json`, checksum manifests, touched-file manifests, or cumulative snapshot manifests.

The rule is skipped unless the component profile declares release evidence as required.
