component:
  name: Gating
  nameEntity: gating
  namespace: Gating\Gate
  business_prefix: Gate
  database_prefix: gate_
  route_owner_root: gate
  route_owner_roots:
    - gate
    - gating
  typed_layers_required: true
  service_interface_mirror: true
  mutation_mode: forbidden
  mutation_scan_excluded_paths:
    - .consuming/**

  secret_scan: enabled
  docblock_preservation: enabled
  release:
    evidence_required: false
    required_files:
      - MANIFEST.json
      - composer.json
