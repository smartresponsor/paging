component:
  name: Messaging
  namespace: App\Messaging
  business_prefix: Message
  database_prefix: message_
  enabled_rules:
    - structure.forbidden_architecture
    - namespace.profile_match
    - layer.typed_class_location
    - mirror.service_interface
    - composer.platform_constraint
    - database.table_prefix
    - mutation.safety_firewall

  route_owner_root: message
  route_owner_roots:
    - message
  mutation_mode: forbidden

  secret_scan: enabled
  release:
    evidence_required: false
    required_files:
      - MANIFEST.json
      - composer.json
