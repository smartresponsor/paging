page_routes:
  resource: routes/page_routes.yaml

page_admin_routes:
  resource: routes/easyadmin.yaml

# Explicit Page routes take priority over the generic CRUD delivery routes.
cruding_routes:
  resource: '@CrudingBundle/config/routes.yaml'
